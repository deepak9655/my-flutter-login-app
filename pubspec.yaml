name: repair_shop_native
description: Repair Shop with Google Drive, Excel export, and animations
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dynamic_color: ^1.0.0
  
  # Existing App Packages
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  path_provider: ^2.0.15
  path: ^1.8.3
  image_picker: ^0.8.7+6
  printing: ^5.10.0
  intl: ^0.18.1
  google_sign_in: ^6.1.4
  googleapis: ^12.0.0
  http: ^0.13.6
  excel: ^2.0.3
  animations: ^2.0.6
  pdf: ^3.10.4

  # ⚙️ NEW DATABASE (ISAR)
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  
  # Settings Storage
  shared_preferences: ^2.2.2
  
  # Sharing and URL Launcher
  share_plus: ^7.2.2
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
    
  # ⚙️ CODE GENERATION
  build_runner: ^2.4.8
  isar_generator: ^3.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
